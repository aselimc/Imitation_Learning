Testing can be started via python -m test
Training can be started via python -m main

I have re-written the code schema, 
almost exactly like in the exercise schema but with few changes.

Also testing script needs to be adjusted if other history types want
to be tested. Currently, it is adjust for history length = 5